# Infra Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Mettre en place l'infrastructure Docker complète (PostgreSQL, MinIO, tous les services) avec un docker-compose fonctionnel en local.

**Architecture:** Monorepo, un seul docker-compose.yml orchestre tous les services. PostgreSQL unique avec schémas séparés par service. MinIO pour le stockage objet local (équivalent S3).

**Tech Stack:** Docker, docker-compose, PostgreSQL 16, MinIO, Go 1.22, Python 3.11, .env pour la config locale.

---

## File Map

```
Mogged/
├── docker-compose.yml
├── .env.example
├── .env                          # ignoré par git
├── .gitignore
├── infra/
│   └── postgres/
│       └── init.sql
├── services/
│   ├── api-gateway/
│   │   ├── Dockerfile
│   │   └── main.go               # serveur HTTP minimal (health check)
│   ├── auth-service/
│   │   ├── Dockerfile
│   │   └── main.go
│   ├── user-service/
│   │   ├── Dockerfile
│   │   └── main.go
│   ├── elo-service/
│   │   ├── Dockerfile
│   │   └── main.go
│   └── face-service/
│       ├── Dockerfile
│       ├── requirements.txt
│       └── main.py               # FastAPI minimal (health check)
```

---

### Task 1: Gitignore et structure de base

**Files:**
- Create: `.gitignore`
- Create: `.env.example`

- [ ] **Step 1: Créer .gitignore**

```
# Env
.env

# Go
services/*/bin/
*.exe

# Python
__pycache__/
*.pyc
.venv/
ai/models/*.joblib
ai/datasets/

# Docker
.docker/

# IDE
.idea/
.vscode/
```

Écrire ce contenu dans `.gitignore` à la racine.

- [ ] **Step 2: Créer .env.example**

```env
# PostgreSQL
POSTGRES_USER=mogged
POSTGRES_PASSWORD=mogged_secret
POSTGRES_DB=mogged

# MinIO
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin123
MINIO_BUCKET=mogged-photos

# JWT
JWT_SECRET=change_me_in_production

# Google OAuth
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret

# Services URLs (internes Docker)
AUTH_SERVICE_URL=http://auth-service:8081
USER_SERVICE_URL=http://user-service:8082
ELO_SERVICE_URL=http://elo-service:8083
FACE_SERVICE_URL=http://face-service:8000

# AI Model
MODEL_PATH=/app/models/model_v1.joblib
```

Copier `.env.example` en `.env` :
```bash
cp .env.example .env
```

- [ ] **Step 3: Commit**

```bash
git add .gitignore .env.example
git commit -m "chore: add gitignore and env template"
```

---

### Task 2: Init SQL — schémas PostgreSQL

**Files:**
- Create: `infra/postgres/init.sql`

- [ ] **Step 1: Créer init.sql**

```sql
-- Schéma auth
CREATE SCHEMA IF NOT EXISTS auth;

CREATE TABLE IF NOT EXISTS auth.users (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    google_id VARCHAR(255) UNIQUE NOT NULL,
    email VARCHAR(255) UNIQUE NOT NULL,
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW()
);

CREATE TABLE IF NOT EXISTS auth.refresh_tokens (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID REFERENCES auth.users(id) ON DELETE CASCADE,
    token TEXT NOT NULL,
    expires_at TIMESTAMP NOT NULL,
    created_at TIMESTAMP DEFAULT NOW()
);

-- Schéma users
CREATE SCHEMA IF NOT EXISTS users;

CREATE TABLE IF NOT EXISTS users.profiles (
    id UUID PRIMARY KEY REFERENCES auth.users(id) ON DELETE CASCADE,
    username VARCHAR(100) NOT NULL,
    avatar_url TEXT,
    consent_ai BOOLEAN DEFAULT FALSE,
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW()
);

CREATE TABLE IF NOT EXISTS users.photos (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID REFERENCES auth.users(id) ON DELETE CASCADE,
    s3_key TEXT NOT NULL,
    chad_score FLOAT,
    features JSONB,
    hash VARCHAR(64) UNIQUE NOT NULL,
    used BOOLEAN DEFAULT FALSE,
    uploaded_at TIMESTAMP DEFAULT NOW()
);

CREATE TABLE IF NOT EXISTS users.upload_rate (
    user_id UUID REFERENCES auth.users(id) ON DELETE CASCADE,
    uploaded_at TIMESTAMP DEFAULT NOW()
);

-- Schéma elo
CREATE SCHEMA IF NOT EXISTS elo;

CREATE TABLE IF NOT EXISTS elo.scores (
    user_id UUID PRIMARY KEY REFERENCES auth.users(id) ON DELETE CASCADE,
    elo INT DEFAULT 1000,
    tier VARCHAR(50) DEFAULT 'Bronze',
    updated_at TIMESTAMP DEFAULT NOW()
);

CREATE TABLE IF NOT EXISTS elo.matches (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    player_a UUID REFERENCES auth.users(id),
    player_b UUID REFERENCES auth.users(id),
    winner UUID REFERENCES auth.users(id),
    elo_change_a INT,
    elo_change_b INT,
    mode VARCHAR(10) NOT NULL DEFAULT 'realtime', -- 'realtime' | 'async'
    status VARCHAR(10) NOT NULL DEFAULT 'pending', -- 'pending' | 'ready' | 'completed' | 'expired'
    expires_at TIMESTAMP,                          -- NULL pour realtime, NOW()+24h pour async
    played_at TIMESTAMP DEFAULT NOW()
);

CREATE TABLE IF NOT EXISTS elo.rounds (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    match_id UUID REFERENCES elo.matches(id) ON DELETE CASCADE,
    photo_a UUID REFERENCES users.photos(id),
    photo_b UUID REFERENCES users.photos(id),
    winner_photo UUID REFERENCES users.photos(id),
    round_number INT NOT NULL
);

-- Schéma face
CREATE SCHEMA IF NOT EXISTS face;

CREATE TABLE IF NOT EXISTS face.scores (
    photo_hash VARCHAR(64) PRIMARY KEY,
    photo_id UUID REFERENCES users.photos(id) ON DELETE CASCADE,
    chad_score FLOAT NOT NULL,
    symmetry FLOAT,
    golden_ratio FLOAT,
    jawline FLOAT,
    eyes FLOAT,
    nose FLOAT,
    forehead FLOAT,
    analyzed_at TIMESTAMP DEFAULT NOW()
);
```

- [ ] **Step 2: Commit**

```bash
git add infra/postgres/init.sql
git commit -m "chore: add postgres schema init sql"
```

---

### Task 3: Dockerfiles des services Go

**Files:**
- Create: `services/api-gateway/Dockerfile`
- Create: `services/auth-service/Dockerfile`
- Create: `services/user-service/Dockerfile`
- Create: `services/elo-service/Dockerfile`

Même Dockerfile pour chaque service Go, adapter `SERVICE_NAME` :

- [ ] **Step 1: Créer les 4 Dockerfiles Go**

Contenu identique pour chaque service (remplacer le chemin) :

`services/api-gateway/Dockerfile` :
```dockerfile
FROM golang:1.22-alpine AS builder
WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN go build -o bin/server ./cmd/server

FROM alpine:3.19
WORKDIR /app
COPY --from=builder /app/bin/server .
EXPOSE 8080
CMD ["./server"]
```

`services/auth-service/Dockerfile` (EXPOSE 8081) :
```dockerfile
FROM golang:1.22-alpine AS builder
WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN go build -o bin/server ./cmd/server

FROM alpine:3.19
WORKDIR /app
COPY --from=builder /app/bin/server .
EXPOSE 8081
CMD ["./server"]
```

`services/user-service/Dockerfile` (EXPOSE 8082) :
```dockerfile
FROM golang:1.22-alpine AS builder
WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN go build -o bin/server ./cmd/server

FROM alpine:3.19
WORKDIR /app
COPY --from=builder /app/bin/server .
EXPOSE 8082
CMD ["./server"]
```

`services/elo-service/Dockerfile` (EXPOSE 8083) :
```dockerfile
FROM golang:1.22-alpine AS builder
WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN go build -o bin/server ./cmd/server

FROM alpine:3.19
WORKDIR /app
COPY --from=builder /app/bin/server .
EXPOSE 8083
CMD ["./server"]
```

- [ ] **Step 2: Commit**

```bash
git add services/api-gateway/Dockerfile services/auth-service/Dockerfile services/user-service/Dockerfile services/elo-service/Dockerfile
git commit -m "chore: add dockerfiles for go services"
```

---

### Task 4: Serveurs Go minimaux (health check)

**Files:**
- Create: `services/api-gateway/go.mod`
- Create: `services/api-gateway/cmd/server/main.go`
- Create: `services/auth-service/go.mod`
- Create: `services/auth-service/cmd/server/main.go`
- Create: `services/user-service/go.mod`
- Create: `services/user-service/cmd/server/main.go`
- Create: `services/elo-service/go.mod`
- Create: `services/elo-service/cmd/server/main.go`

- [ ] **Step 1: Créer go.mod pour chaque service**

`services/api-gateway/go.mod` :
```
module github.com/mogged/api-gateway

go 1.22
```

`services/auth-service/go.mod` :
```
module github.com/mogged/auth-service

go 1.22
```

`services/user-service/go.mod` :
```
module github.com/mogged/user-service

go 1.22
```

`services/elo-service/go.mod` :
```
module github.com/mogged/elo-service

go 1.22
```

- [ ] **Step 2: Créer main.go minimal pour chaque service**

`services/api-gateway/cmd/server/main.go` :
```go
package main

import (
	"fmt"
	"net/http"
	"os"
)

func main() {
	port := os.Getenv("PORT")
	if port == "" {
		port = "8080"
	}
	http.HandleFunc("/health", func(w http.ResponseWriter, r *http.Request) {
		fmt.Fprintln(w, `{"status":"ok","service":"api-gateway"}`)
	})
	http.ListenAndServe(":"+port, nil)
}
```

`services/auth-service/cmd/server/main.go` (service "auth-service", port 8081) :
```go
package main

import (
	"fmt"
	"net/http"
	"os"
)

func main() {
	port := os.Getenv("PORT")
	if port == "" {
		port = "8081"
	}
	http.HandleFunc("/health", func(w http.ResponseWriter, r *http.Request) {
		fmt.Fprintln(w, `{"status":"ok","service":"auth-service"}`)
	})
	http.ListenAndServe(":"+port, nil)
}
```

`services/user-service/cmd/server/main.go` (service "user-service", port 8082) :
```go
package main

import (
	"fmt"
	"net/http"
	"os"
)

func main() {
	port := os.Getenv("PORT")
	if port == "" {
		port = "8082"
	}
	http.HandleFunc("/health", func(w http.ResponseWriter, r *http.Request) {
		fmt.Fprintln(w, `{"status":"ok","service":"user-service"}`)
	})
	http.ListenAndServe(":"+port, nil)
}
```

`services/elo-service/cmd/server/main.go` (service "elo-service", port 8083) :
```go
package main

import (
	"fmt"
	"net/http"
	"os"
)

func main() {
	port := os.Getenv("PORT")
	if port == "" {
		port = "8083"
	}
	http.HandleFunc("/health", func(w http.ResponseWriter, r *http.Request) {
		fmt.Fprintln(w, `{"status":"ok","service":"elo-service"}`)
	})
	http.ListenAndServe(":"+port, nil)
}
```

- [ ] **Step 3: Commit**

```bash
git add services/
git commit -m "chore: add minimal go health check servers"
```

---

### Task 5: Dockerfile et app Python minimale (face-service)

**Files:**
- Create: `services/face-service/Dockerfile`
- Create: `services/face-service/requirements.txt`
- Create: `services/face-service/main.py`

- [ ] **Step 1: Créer requirements.txt**

```
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-dotenv==1.0.1
```

- [ ] **Step 2: Créer main.py minimal**

```python
from fastapi import FastAPI

app = FastAPI()

@app.get("/health")
def health():
    return {"status": "ok", "service": "face-service"}
```

- [ ] **Step 3: Créer Dockerfile**

```dockerfile
FROM python:3.11-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY . .
EXPOSE 8000
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
```

- [ ] **Step 4: Commit**

```bash
git add services/face-service/
git commit -m "chore: add minimal face-service fastapi app"
```

---

### Task 6: docker-compose.yml

**Files:**
- Create: `docker-compose.yml`

- [ ] **Step 1: Créer docker-compose.yml**

```yaml
version: "3.9"

services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./infra/postgres/init.sql:/docker-entrypoint-initdb.d/init.sql
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER}"]
      interval: 5s
      timeout: 5s
      retries: 5

  minio:
    image: minio/minio
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD}
    volumes:
      - minio_data:/data
    ports:
      - "9000:9000"
      - "9001:9001"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 10s
      timeout: 5s
      retries: 5

  api-gateway:
    build: ./services/api-gateway
    ports:
      - "8080:8080"
    environment:
      PORT: "8080"
      JWT_SECRET: ${JWT_SECRET}
      AUTH_SERVICE_URL: ${AUTH_SERVICE_URL}
      USER_SERVICE_URL: ${USER_SERVICE_URL}
      ELO_SERVICE_URL: ${ELO_SERVICE_URL}
      FACE_SERVICE_URL: ${FACE_SERVICE_URL}
    depends_on:
      postgres:
        condition: service_healthy

  auth-service:
    build: ./services/auth-service
    ports:
      - "8081:8081"
    environment:
      PORT: "8081"
      POSTGRES_DSN: postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}?sslmode=disable
      JWT_SECRET: ${JWT_SECRET}
      GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID}
      GOOGLE_CLIENT_SECRET: ${GOOGLE_CLIENT_SECRET}
    depends_on:
      postgres:
        condition: service_healthy

  user-service:
    build: ./services/user-service
    ports:
      - "8082:8082"
    environment:
      PORT: "8082"
      POSTGRES_DSN: postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}?sslmode=disable
      MINIO_ENDPOINT: minio:9000
      MINIO_ROOT_USER: ${MINIO_ROOT_USER}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD}
      MINIO_BUCKET: ${MINIO_BUCKET}
    depends_on:
      postgres:
        condition: service_healthy
      minio:
        condition: service_healthy

  elo-service:
    build: ./services/elo-service
    ports:
      - "8083:8083"
    environment:
      PORT: "8083"
      POSTGRES_DSN: postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}?sslmode=disable
    depends_on:
      postgres:
        condition: service_healthy

  face-service:
    build: ./services/face-service
    ports:
      - "8000:8000"
    environment:
      POSTGRES_DSN: postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}?sslmode=disable
      MINIO_ENDPOINT: minio:9000
      MINIO_ROOT_USER: ${MINIO_ROOT_USER}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD}
      MINIO_BUCKET: ${MINIO_BUCKET}
      MODEL_PATH: ${MODEL_PATH}
    depends_on:
      postgres:
        condition: service_healthy
      minio:
        condition: service_healthy
    volumes:
      - ./ai/models:/app/models:ro

volumes:
  postgres_data:
  minio_data:
```

- [ ] **Step 2: Commit**

```bash
git add docker-compose.yml
git commit -m "chore: add docker-compose with all services"
```

---

### Task 7: Vérification — tout démarre

- [ ] **Step 1: Builder et démarrer tous les services**

```bash
docker compose up --build -d
```

Attendre ~30 secondes.

- [ ] **Step 2: Vérifier que tous les containers sont UP**

```bash
docker compose ps
```

Attendu : tous les services en `running` ou `healthy`.

- [ ] **Step 3: Tester les health checks**

```bash
curl http://localhost:8080/health
# {"status":"ok","service":"api-gateway"}

curl http://localhost:8081/health
# {"status":"ok","service":"auth-service"}

curl http://localhost:8082/health
# {"status":"ok","service":"user-service"}

curl http://localhost:8083/health
# {"status":"ok","service":"elo-service"}

curl http://localhost:8000/health
# {"status":"ok","service":"face-service"}
```

- [ ] **Step 4: Vérifier que les schémas PostgreSQL existent**

```bash
docker compose exec postgres psql -U mogged -d mogged -c "\dn"
```

Attendu : schémas `auth`, `users`, `elo`, `face` listés.

- [ ] **Step 5: Arrêter les services**

```bash
docker compose down
```

- [ ] **Step 6: Commit final**

```bash
git add .
git commit -m "chore: infra complete — all services boot with health checks"
```
